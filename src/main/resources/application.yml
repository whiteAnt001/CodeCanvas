server:
  port: 8099

spring:
  datasource:
    url: jdbc:h2:file:~/testdb
    driver-class-name: org.h2.Driver
    username: sa
    password:
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database-platform: org.hibernate.dialect.H2Dialect
  multipart:
    max-file-size: 10MB
    max-request-size: 10MB
  mail:
    host: smtp.gmail.com
    port: 587
    username: westjun01@gmail.com   # Gmail 주소(일단 개인요ㅕ으로 넣음)
    password: akcs buwr uigc jhtr      # 앱 비밀번호
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
